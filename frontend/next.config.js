/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: In production, use environment variable for API URL
  // For development, API calls go directly to Django backend
};

module.exports = nextConfig;

